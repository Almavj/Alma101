:80
root * ./
encode gzip zstd

# API requests — strip the /api prefix and serve from ./backend
handle_path /api/* {
    root * ./backend
    php
    file_server
}

# Everything else - SPA
handle {
    try_files {path} {path}/ /index.html
    file_server
}

:80
root * ./
encode gzip zstd

# API requests — proxy /api/* to backend container (do not try to run php here)
handle /api/* {
    reverse_proxy 127.0.0.1:8000
}

# Everything else - SPA
handle {
    try_files {path} {path}/ /index.html
    file_server
}
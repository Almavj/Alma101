{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && composer install --no-dev --prefer-dist --optimize-autoloader --no-interaction && cd .. && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "caddy run --config /Caddyfile --adapter caddyfile"
  }
}